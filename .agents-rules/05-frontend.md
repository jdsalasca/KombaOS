Frontend
- Use hooks for state, effects, and forms.
- Keep validations clear and user-friendly.
- Build small, reusable components.
- Keep business logic out of presentational components.

Do
- Separate container and presentational components.
- Reuse existing feature components and types.

Don’t
- Don’t fetch data directly in UI-only components.
